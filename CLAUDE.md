@AGENTS.md

Claude Code doesn't read AGENTS.md natively, so this file imports it directly rather
than duplicating its content — see AGENTS.md for build/test/lint commands, conventions,
and project-specific rules.
